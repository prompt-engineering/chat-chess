export enum RoomStatusEnum {
  WAITING_TO_START = 0,
  STARTED = 1,
  WAITING_PLAYER = 2,
  ENDED = 3
};

export enum PlayerTypeEnum {
  HUMAN = 0,
  AI = 1
};

export enum MessageTypeEnum {
  PUBLIC = 0,
  PRIVATE = 1
};